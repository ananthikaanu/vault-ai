services:
  - type: web
    name: vault-ai-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3001
    disk:
      name: vault-data
      mountPath: /opt/render/project/src/backend
      sizeGB: 1
